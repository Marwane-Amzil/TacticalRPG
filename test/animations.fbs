table animations_root
{
    animations_sets: [set];
}

table set
{
    type: string (key);
    animations: [animation];
}

table animation
{
    name: string;
    frames: [frame];
}

table frame
{
    left: uint32;
    top: uint32;
    width: uint32;
    height: uint32;
}

root_type animations_root;